import Input from '@/components/astra/Input';
import ComponentWrapper from '@/components/ComponentWrapper';
import CommandLine from '@/components/astra/CommandLine';

# Input

A sleek, customizable input component designed for building responsive and accessible forms with ease.

<div className="my-8 flex flex-col gap-4">
  <ComponentWrapper>
    <Input />
  </ComponentWrapper>
</div>

## Installation

Install the input component from Astra UI using your preferred package manager for fast integration.

<div className="my-8 flex flex-col gap-4">
  <CommandLine command="npx astui@latest add input" />
  <CommandLine command="pnpm dlx astui@latest add input" />
</div>

## Usage

A flexible input field ready for use in forms, filters, and settings. Easily extendable with labels, icons, or validation.

<div className="my-8 flex flex-col gap-4">
  <CommandLine language="tsx" command='import Input from "@/components/astra/Input"' />
  <CommandLine language="tsx" command='<Input type="file" id="user-image" placeholder="Upload your image" disabled required className="bg-blue-500" onChange={handleChange} />' />
</div>